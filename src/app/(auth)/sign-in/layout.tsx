import SignIn from "./page";

export const metadata = {
  title: "Login | Minimal Kings IT",
  description: "Form di accesso per admin",
};

export default function SignInLayout() {
  return <SignIn />;
}
